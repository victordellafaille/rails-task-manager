<h1>Your tasks</h1>

<%= link_to "Create new task", new_task_path %>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> <br>
    <%= link_to "delete this task", task_path(task), method: :delete, data: {confirm: "Are you sure?"} %> </li>
  <% end %>
</ul>
